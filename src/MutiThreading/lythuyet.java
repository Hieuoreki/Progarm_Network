package MutiThreading;

public class lythuyet {
	
	// Thread: là 1 tiến trình con nhằm thực hiện một nhiệm vụ nào đó
	// Cách Thread làm việc: chương trình chạy nhanh hơn
	// Ưu điểm: Nhanh, chia sẻ cùng nguồn tài nguyên
	// Nhược điểm: Xung đột bộ nhớ
	// Các trạng thái: New, Runnable, Block, Waiting, Terminate
	// Lớp Thread kế thừa lớp Object và interface Runnable
	
	// Một số phương thức phổ biến
	// start(): -Thread-
	// run() : -Runnable, Thread-
	// sleep(): -Thread- Đưa Thread về trạng thái wait để thực hiện Thread khác
	// wait(): trạng thái chờ
	// notify(): Đánh thức 1 Thread
	// notifyAll(): Đánh thức tất cả Thread
	
	
	// Định danh tuyến (Thread ID)
	// Tên tuyến (ThreadName)
	
	
	// Độ ưu tiên tuyến (Thread's priority)
	// Thread scheduler:
	// Quyết định Thread nào đc thực thi tại từng thười điểm
	
	// Thuâtj toán ưu tiên
	


}
